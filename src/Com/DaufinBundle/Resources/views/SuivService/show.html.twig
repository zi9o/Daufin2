{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SuivService</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Prixht</th>
                <td>{{ entity.prixHt }}</td>
            </tr>
            <tr>
                <th>Tva</th>
                <td>{{ entity.tva }}</td>
            </tr>
            <tr>
                <th>Prixttc</th>
                <td>{{ entity.prixTtc }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('com_suiv_service') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('com_suiv_service_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
