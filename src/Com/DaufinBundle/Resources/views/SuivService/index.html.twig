{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SuivService list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prixht</th>
                <th>Tva</th>
                <th>Prixttc</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('com_suiv_service_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.prixHt }}</td>
                <td>{{ entity.tva }}</td>
                <td>{{ entity.prixTtc }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('com_suiv_service_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('com_suiv_service_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('com_suiv_service_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
