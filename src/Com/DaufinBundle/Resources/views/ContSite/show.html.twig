{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ContSite</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Typesite</th>
                <td>{{ entity.typeSite }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('com_Cont_Site') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('com_Cont_Site_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
