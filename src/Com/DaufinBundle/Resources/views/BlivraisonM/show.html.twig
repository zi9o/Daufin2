{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlivraisonM</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Numbl</th>
                <td>{{ entity.numBl }}</td>
            </tr>
            <tr>
                <th>Titreblm</th>
                <td>{{ entity.titreBlM }}</td>
            </tr>
            <tr>
                <th>Dateblm</th>
                <td>{{ entity.dateBlM|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('com_BLivraison') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('com_BLivraison_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
