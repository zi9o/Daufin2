{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PasserPar</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Datepasser</th>
                <td>{{ entity.datePasser|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('com_PasserPar') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('com_PasserPar_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
